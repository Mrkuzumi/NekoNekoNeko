# Vim编辑器使用指南与Linux高频指令（适配Word复制）

# 一、Vim 编辑器使用技巧（高频核心操作）

### 说明：Vim 是 Linux 系统自带的强大文本编辑器，核心分为三种模式，需熟练切换模式操作。

## （一）基础模式切换

1.  启动 Vim 与退出

vim 文件名 （启动 Vim 并打开指定文件，文件不存在则创建）

示例：vim test.txt

2.  模式切换核心快捷键

→ 启动后默认进入「普通模式」，按 i 键切换到「插入模式」（可编辑文本）

→ 插入模式下按 Esc 键，退回「普通模式」（可执行快捷键操作）

→ 普通模式下输入 : 切换到「命令行模式」（执行保存、退出等指令）

## （二）普通模式高频操作（核心快捷键）

### 光标移动

1.  上下左右移动：h（左）、j（下）、k（上）、l（右）（无需鼠标，高效操作）

2.  快速定位：gg（跳至文件开头）、G（跳至文件结尾）、nG（跳至第n行，如 5G 跳至第5行）

3.  行内定位：^（跳至行首非空字符）、$（跳至行尾）、w（跳至下一个单词开头）、b（跳至上一个单词开头）

### 文本编辑

1.  删除操作：x（删除光标所在字符）、dd（删除光标所在行，常用）、ndd（删除从当前行开始的n行，如 3dd 删除3行）

2.  复制粘贴：yy（复制光标所在行）、nyy（复制n行）、p（粘贴到光标下一行）、P（粘贴到光标上一行）

3.  撤销与恢复：u（撤销上一步操作）、Ctrl+r（恢复撤销的操作）

4.  替换操作：r（替换光标所在单个字符，按r后输入新字符）、R（进入替换模式，持续替换后续字符，Esc退出）

## （三）命令行模式常用指令（: 开头）

1.  保存与退出

:w （保存文件，不退出）

:q （退出Vim，文件无修改时可用）

:wq （保存并退出，常用）

:q! （强制退出，不保存修改，谨慎使用）

:w 新文件名 （另存为新文件，保留原文件）

2.  查找与替换

:/关键词 （从光标处向下查找关键词，按n查看下一个，N查看上一个）

:?关键词 （从光标处向上查找关键词）

:%s/旧内容/新内容/g （全局替换所有旧内容为新内容，%代表全部行，g代表全局）

:n1,n2s/旧内容/新内容/g （替换第n1行到n2行的旧内容，如 :5,10s/name/Name/g）

3.  其他实用指令

:set nu （显示行号）、:set nonu （隐藏行号）

:set ts=4 （设置制表符Tab宽度为4个空格）

## （四）Vim 实用技巧

1.  批量缩进：普通模式下选中多行（按V进入可视行模式，上下键选行），按 > 缩进，< 取消缩进。

2.  临时粘贴：插入模式下按 Shift+Insert 粘贴系统剪贴板内容（Linux环境）。

3.  强制保存：若文件权限不足，:w! （强制保存，需有对应权限）。

# 二、Linux 高频指令（日常运维与开发必备）

## （一）文件与目录操作（最常用）

1.  目录切换：cd 目录路径

cd ~ （切换到当前用户主目录）、cd .. （切换到上一级目录）、cd / （切换到根目录）、cd - （切换到上一次所在目录）

2.  查看目录内容：ls

ls -l （简写 ll，详细格式显示，包含权限、大小、修改时间等）

ls -a （显示所有文件，包括隐藏文件（以.开头））

ls -lh （人性化显示文件大小，如 KB、MB）

3.  创建与删除

mkdir 目录名 （创建单个目录）、mkdir -p 目录1/目录2 （递归创建多级目录）

touch 文件名 （创建空文件，若文件存在则更新修改时间）

rm 文件名 （删除文件，需确认）、rm -f 文件名 （强制删除文件，不确认）

rm -r 目录名 （递归删除目录及内部所有内容）、rm -rf 目录名 （强制删除目录，谨慎使用，不可恢复）

4.  复制与移动

cp 源文件 目标路径 （复制文件）、cp -r 源目录 目标路径 （复制目录）

mv 源文件/目录 目标路径 （移动文件/目录，可用于重命名）

示例：mv old.txt new.txt （将old.txt重命名为new.txt）

5.  查看文件内容

cat 文件名 （一次性显示整个文件内容，适合小文件）

more 文件名 （分页显示文件，按空格翻页，q退出）

less 文件名 （分页显示，支持上下键滚动、查找，q退出，适合大文件）

head -n 文件名 （显示文件前n行，默认前10行）、tail -n 文件名 （显示文件后n行）

tail -f 文件名 （实时监控文件内容变化，常用于查看日志，Ctrl+c退出）

## （二）权限管理

1.  查看权限：ls -l （权限字段格式：rwxrwxrwx，分属所有者、所属组、其他用户）

r（读权限，4）、w（写权限，2）、x（执行权限，1），无权限为 -

2.  修改权限：chmod

chmod 755 文件名 （所有者rwx，所属组rx，其他用户rx，常用可执行文件权限）

chmod 644 文件名 （所有者rw，其他用户r，常用普通文件权限）

chmod +x 文件名 （给文件添加执行权限，常用）、chmod -w 文件名 （移除写权限）

3.  修改所有者：chown

chown 用户名:组名 文件名/目录 （修改所有者和所属组）

示例：chown user:user test.txt

## （三）系统与进程管理

1.  查看系统信息

uname -a （查看系统内核版本、主机名、架构等详细信息）

df -h （查看磁盘空间使用情况，人性化显示）

free -h （查看内存使用情况，包括物理内存、交换内存）

top （实时查看系统进程、CPU、内存占用，按q退出）、ps aux （查看所有进程详细信息）

2.  进程操作

kill -9 进程ID （强制终止指定ID的进程，常用，先通过ps查找进程ID）

killall 进程名 （终止所有同名进程）

nohup 命令 & （后台运行命令，断开终端不终止，输出日志到nohup.out）

## （四）网络操作

1.  查看网络配置：ifconfig （或 ip addr，查看网卡IP、MAC地址等）

2.  测试网络连通性：ping 目标IP/域名 （按Ctrl+c停止，检测是否可达）

3.  查看端口占用：netstat -tuln （查看监听端口）、netstat -tulnp （查看端口对应的进程）

4.  远程连接：ssh 用户名@目标IP （远程登录Linux服务器）

示例：ssh user@192.168.1.100

## （五）其他高频指令

1.  压缩与解压

tar -zcvf 压缩包名.tar.gz 源文件/目录 （打包并gzip压缩）

tar -zxvf 压缩包名.tar.gz （解压gzip压缩包，x表示解压，v显示过程）

unzip 压缩包名.zip （解压zip压缩包）、zip 压缩包名.zip 源文件/目录 （打包zip压缩包）

2.  查找文件与内容

find 查找路径 -name "文件名" （按文件名查找文件）

示例：find /home -name "*.txt" （在/home目录下查找所有.txt文件）

grep "关键词" 文件名 （在文件中查找包含关键词的行）

grep -r "关键词" 目录 （递归查找目录下所有文件中包含关键词的内容）

3.  管道与重定向

命令1 | 命令2 （将命令1的输出作为命令2的输入，常用）

示例：ls -l | grep "txt" （查看当前目录下所有.txt文件的详细信息）

命令 > 文件名 （将命令输出重定向到文件，覆盖原有内容）

命令 >> 文件名 （将命令输出追加到文件末尾，不覆盖原有内容）

# 三、核心使用总结

1.  Vim 核心：牢记「普通模式（快捷键）→ 插入模式（编辑）→ 命令行模式（保存退出）」的切换逻辑，熟练基础快捷键可大幅提升效率。

2.  Linux 指令：优先掌握文件目录操作、权限管理、进程与网络指令，覆盖日常开发与运维80%以上场景，复杂场景可通过 man 指令（如 man ls）查看详细说明。

3.  注意事项：Linux 指令区分大小写，删除操作（rm -rf）需谨慎；Vim 若误操作，可通过 u 撤销，强制退出用 :q!。

# 三、Tmux 终端复用工具使用指南（高频核心操作）

### 说明：Tmux 是 Linux 下强大的终端复用工具，可实现多终端窗口管理、会话保持（断开连接后进程仍运行），适合远程操作与多任务并行。

## （一）基础概念

1.  会话（Session）：Tmux 的独立运行环境，可包含多个窗口，断开连接后会话可后台保持。

2.  窗口（Window）：单个会话内的独立终端界面，可切换显示，类似浏览器标签页。

3.  面板（Pane）：单个窗口内的分栏，可同时显示多个终端会话，实现多任务并行查看。

4.  前缀键：Tmux 大部分操作需先按前缀键（默认 Ctrl+b），再按对应指令键，核心操作逻辑。

## （二）会话管理（核心操作）

1.  启动与退出

tmux （启动默认会话，自动创建一个窗口和一个面板）

tmux new -s 会话名 （创建指定名称的会话，推荐，便于管理）

示例：tmux new -s dev （创建名为 dev 的会话）

exit （退出当前面板/窗口，所有面板退出则会话结束）

2.  会话查看与切换

tmux ls （查看所有后台会话，外部终端或 Tmux 内均可执行）

tmux attach -t 会话名/会话ID （连接到指定后台会话）

示例：tmux attach -t dev （连接到 dev 会话）、tmux attach -t 0 （连接到ID为0的会话）

前缀键 + s （Tmux 内交互式切换会话，按方向键选择，回车确认）

3.  会话分离与删除

前缀键 + d （分离当前会话，会话后台运行，返回外部终端）

tmux kill-session -t 会话名/会话ID （删除指定会话，谨慎操作）

示例：tmux kill-session -t dev （删除 dev 会话）

tmux kill-server （终止所有 Tmux 会话，谨慎使用）

## （三）窗口管理

1.  创建与切换窗口

前缀键 + c （在当前会话内创建新窗口，默认命名为序号）

前缀键 + 数字键（0-9） （切换到对应序号的窗口，窗口序号从0开始）

前缀键 + n （切换到下一个窗口）、前缀键 + p （切换到上一个窗口）

前缀键 + w （交互式查看所有窗口，按方向键选择，回车确认）

2.  窗口重命名与关闭

前缀键 + , （重命名当前窗口，便于识别，推荐）

示例：将窗口重命名为 vim-edit，方便区分任务

前缀键 + & （关闭当前窗口，所有面板关闭则窗口消失，需确认）

## （四）面板管理（分栏操作，高频实用）

1.  分割面板

前缀键 + % （垂直分割面板，左右分栏）

前缀键 + " （水平分割面板，上下分栏）

2.  面板切换与调整

前缀键 + 方向键（上/下/左/右） （切换到对应方向的面板）

前缀键 + ; （切换到上一次操作的面板）

前缀键 + Alt+方向键 （调整当前面板大小，按需微调分栏比例）

3.  面板关闭与合并

前缀键 + x （关闭当前面板，需确认，最后一个面板关闭则窗口消失）

前缀键 + ! （将当前面板拆分为独立窗口，便于单独操作）

## （五）Tmux 实用技巧与配置

1.  复制模式（Tmux 内复制文本）

前缀键 + [ （进入复制模式，按方向键移动光标选择文本）

按空格开始选择，选中文本后按 Enter 复制，前缀键 + ] （粘贴复制的文本）

2.  常用快捷键汇总（快速记忆）

前缀键 + d （分离会话）、前缀键 + c （新建窗口）、前缀键 + %/" （分割面板）

前缀键 + 数字键（切换窗口）、前缀键 + 方向键（切换面板）、前缀键 + x （关闭面板）

3.  配置持久化（可选）

编辑配置文件：vim ~/.tmux.conf （无则创建，自定义前缀键、面板样式等）

示例配置（常用）：

set -g prefix C-a （将前缀键改为 Ctrl+a，更顺手）

set -g pane-base-index 1 （面板序号从1开始，避免混淆）

配置生效：tmux source ~/.tmux.conf （无需重启会话，立即生效）

## （六）使用注意事项

1.  远程连接场景：通过 Tmux 启动进程后，即使断开 SSH 连接，进程仍在 Tmux 会话中运行，重新连接后可恢复操作。

2.  快捷键记忆：核心记住前缀键 + 常用指令（会话d、窗口c、面板%/"），即可覆盖80%以上场景。

3.  避免误操作：关闭面板/窗口时注意确认，避免误删重要运行进程。
> （注：文档部分内容可能由 AI 生成）